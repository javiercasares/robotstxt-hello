# robotstxt-hello
Plugin for WordPress as an example.

## Compatibility

The plugin has been verified for compatibility with PHP 5.6 through PHP 8.4 using the PHPCompatibilityWP ruleset.

## Translations

Greetings are localized for 202 locales as listed in the `languages/` directory.
